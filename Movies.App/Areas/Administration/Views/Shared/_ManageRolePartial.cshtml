@model ApplicationRole

<div class="d-flex w-100 pb-xl-2 border-bottom">
    <div class="d-flex justify-content-center flex-column col-7 ms-xl-3">
        <h3>@Model.Name</h3>
    </div>
    <div class="d-flex align-items-center col-5">
        <a href="@Url.Action("DeleteRole", "Admin", new { id = Model.Id })" class="btn btn-danger">Delete</a>
    </div>
</div>